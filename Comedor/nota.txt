

Pendientes

- Aplicar todas la funcionalidades de la auditoria y necesito poder generar pdf y csv
- Verificar los pdf de los reportes.
- Activar todos los servicio de mensajeria y alertas para probar su conjunto.

  <div className="mt-3">
                                <button
                                  className={`btn btn-sm w-100 ${
                                    completado
                                      ? "btn-success fw-bold"
                                      : !todasLasAsistenciasRegistradas()
                                      ? "btn-secondary"
                                      : "btn-success"
                                  }`}
                                  onClick={() =>
                                    marcarServicioCompletado(horario.id)
                                  }
                                  disabled={
                                    !completado &&
                                    !todasLasAsistenciasRegistradas()
                                  }
                                  title={
                                    !todasLasAsistenciasRegistradas() &&
                                    !completado
                                      ? "No se puede completar hasta que se registre toda la asistencia"
                                      : ""
                                  }
                                  style={{
                                    boxShadow: completado
                                      ? "0 4px 12px rgba(40, 167, 69, 0.5)"
                                      : "none",
                                    transform: completado
                                      ? "scale(1.02)"
                                      : "scale(1)",
                                    transition: "all 0.2s ease",
                                  }}
                                >
                                  <i
                                    className={`fas ${
                                      completado
                                        ? "fa-check-circle"
                                        : "fa-circle"
                                    } me-1`}
                                  ></i>
                                  {completado
                                    ? `✅ ${horario.nombre} Completado`
                                    : asistenciaReal[horario.id] > 0
                                    ? `Marcar ${horario.nombre} como Completado`
                                    : !todasLasAsistenciasRegistradas()
                                    ? `⏳ Esperando asistencia para ${horario.nombre}`
                                    : `Marcar ${horario.nombre} como Completado`}
                                </button>
                              </div>