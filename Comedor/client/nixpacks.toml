providers = ["node"]

[phases.install]
cmds = [
    "pnpm install --no-frozen-lockfile", 
    "npm install -g corepack",
    "corepack enable",
    "corepack prepare pnpm@10.15.0 --activate",
    "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["pnpm run build"]